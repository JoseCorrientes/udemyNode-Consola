const nombre = "Deadpool";
const real = "Wade Winston";

const nombreReal = `el nombre de ${nombre} es ${real}`;
console.log(nombreReal);

const html = `

                                        Ejemplo de Template String

                                        Asi funciona

                                        El detalle es asi

`;

console.log(html);